{% extends 'base.html.twig' %}
{% block content %}
    <div class="starter-template">
        <h1>{{ page.title }}</h1>
        <p>
            {{ page.content }}
        </p>
    </div>
{% endblock %}